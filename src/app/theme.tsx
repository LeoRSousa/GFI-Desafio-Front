'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0470dc', // azul padrão
    },
    secondary: {
      main: '#001a49', // roxo
    },
  },
});

export default theme;